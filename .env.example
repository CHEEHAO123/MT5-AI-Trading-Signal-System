# Copy to .env and fill in real values, or export these directly before running ai/analyze.py
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_CHAT_ID_TESTING=
GEMINI_API_KEY=

# MySQL connection (ai/db.py, ai/models.py) — run "alembic upgrade head" against this database once before first use
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
