
Repo review
- Assessed the repo for hiring-manager readiness; found a live Telegram token + Gemini key exposed in git history (public repo).

Added
- LICENSE (MIT)
- Pinned requirements.txt versions

ai/analyze.py — error handling
- Replaced print with real logging
- Fixed Telegram calls: added timeout, no more unhandled crashes
- Fixed a real bug: Gemini retry was catching the wrong exception type, so 429 retries never worked
- Guarded against malformed/partial request JSON
- Added a global Flask error handler (no more raw 500s)

mt5/experts/mt5signal_ai.mq5
- Fixed missing indicator handle checks in OnInit()
- Caught and fixed a regression: hardcoded secrets + broken #ifdef LIVE_MODE toggle had crept back in — restored input params (InpBotToken, InpChatId, InpAiServerBaseUrl, InpLiveMode) and a working runtime live-mode switch

Still open / unresolved
- Confirm whether the re-hardcoded token was ever committed/pushed — if so it needs rotating again
- README's /analyze API doc is stale vs. the current payload fields
- Nothing from today has been committed yet

Rest well — pick up anytime.